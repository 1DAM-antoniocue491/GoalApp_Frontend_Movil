/**
 * Hooks - Exportaciones centralizadas
 *
 * Importa hooks reutilizables desde un solo lugar:
 * @example
 * import { useTheme, useFavorites } from '@/src/hooks';
 */

export { useFavorites, useFavoriteState } from './useFavorites';
