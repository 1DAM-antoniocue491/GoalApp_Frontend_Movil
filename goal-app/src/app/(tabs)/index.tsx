/**
 * Dashboard - Ruta (tabs)/index
 *
 * Thin wrapper que delega toda la logica al feature component.
 * Expo Router requiere export default para el routing basado en archivos.
 */

import DashboardScreen from '@/src/features/dashboard/components/DashboardScreen';

export default DashboardScreen;